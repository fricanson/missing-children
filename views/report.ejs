<% include header.ejs %>
    <% include nav.ejs %>
        <!-- report.js -->
        <div class="container formdiv">
            <div class="form-group">
                <h1>Write your report</h1>
                <p>
                <ol>write using the formart:</ol>
                <ol>URGENT! MISSING CHILD</ol>
                <OL>Name:</OL>
                <ol>OB Number:</ol>
                <ol>More inforamation to be found in Missingchildren.com(Finish with the above)</ol>
                </p>
                <div class="col-report">
                    <form action='/report' method='post'>
                        <textarea name="message" spellcheck="false" placeholder="Type something here..."
                            required></textarea>
                        <script>
                            const textarea = document.querySelector("textarea");
                            textarea.addEventListener("keyup", e => {
                                textarea.style.height = "63px";
                                let scHeight = e.target.scrollHeight;
                                textarea.style.height = `${scHeight}px`;
                            });
                        </script>
                </div>
                <div class="delete-form">
                    <!-- <input type="submit" value="Send report"> -->
                    <button class="btn homebutton delete-button" type="submit">Send Report</button>
                </div>
                </form>
            </div>
        </div>
        <% include footer.ejs %>